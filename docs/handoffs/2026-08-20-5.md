# Handoff — 2026-08-20 (5)

## Bu Oturumda Ne Yapıldı

- İki dış repo incelendi ve bizim projeyle karşılaştırıldı:
  - `mattpocock/skills` — bize gerçekten yakın akraba (aynı kategori: Claude Code skill
    koleksiyonu, benzer ölçek, aynı problemler). İki somut iyileştirme çıkarıldı ve
    uygulandı (aşağıda).
  - `NousResearch/hermes-agent` — bize yakın DEĞİL. Bağımsız otonom ajan ürünü (kendi
    agent loop'u, çoklu platform gateway, vektör hafıza, multi-agent, cloud dağıtım).
    Yaptığı şeylerin çoğu bizim `DECISIONS.md`'de bilinçli olarak REDDETTİĞİMİZ
    şeyler — farklı bir problem için doğru çözümler. Somut olarak alınacak bir şey
    görülmedi, hiçbir değişiklik yapılmadı.
- `mattpocock/skills`'ten gelen iki iyileştirme `skills/handoff/SKILL.md`'ye uygulandı
  (commit `fd9fbfa`):
  1. `disable-model-invocation: true` — model artık `/handoff`'u kendiliğinden
     tetikleyemez, sadece kullanıcı açıkça çağırabilir. Gerekçe: git'e kalıcı yazan,
     geri alınması zor bir işlem, kazara tetiklenmemeli.
  2. Hassas bilgi redaksiyon kuralı — yazmadan önce API key/token/şifre/kişisel kimlik
     varsa `[REDACTED]` ile değiştirilmesi zorunlu. Gerçek bir güvenlik açığıydı:
     handoff dosyaları git'e kalıcı giriyor, konuşmada paylaşılan bir sır farkında
     olmadan commit edilebilirdi.
  Her ikisi de `docs/DECISIONS.md`'ye gerekçesiyle kaydedildi.
- M7 (gerçek proje testi) planlandı ve `PLANS.md`'ye yazıldı — henüz başlanmadı.

## Denendi, Olmadı

- WebFetch ile çekilen repo özetlerinde iki kez gerçekçi olmayan istatistikler çıktı
  (`mattpocock/skills` için "225k star", `hermes-agent` için "233k star"). Kişisel bir
  skill koleksiyonu için bu rakamlar inandırıcı değil, doğrulanamadı, kullanılmadı.
  Ders: WebFetch'in özetleme katmanından gelen sayısal iddialara güvenilmemeli.
- `gh` CLI ile repo incelemesi denendi, kurulu değil (`command not found`), WebFetch'e
  geçildi.
- `raw.githubusercontent.com/.../skills/handoff/SKILL.md` 404 verdi — skill'ler kategori
  klasörleri altında (`skills/productivity/handoff/`), düz değil. GitHub API ile klasör
  yapısı çıkarılıp doğru yol bulundu.

## Alınan Kararlar

- **M7'nin konumu:** test projesi hub'ın ALTINA değil, KARDEŞİ olarak açılacak
  (`C:\Users\batuh\proje-a`). Kullanıcı önce alt klasör önerdi ("hub merkez değil mi?"),
  ama doğrulandı ki kurulan hedef hub'a hiç erişmiyor — `${CLAUDE_PROJECT_DIR}` ve
  `git rev-parse --show-toplevel` hep hedefin kendisini çözümlüyor. Hub bir köken/kaynak,
  kapsayıcı değil (setup.exe benzetmesi). Alt klasör hiçbir teknik fayda getirmezken
  git iç içe geçmesi, `AGENTS.md` sızması ve geçersiz test riski getiriyor.
  Gerekçe `PLANS.md` § M7'de kayıtlı.
- `hermes-agent`'tan bir şey alınmaması bilinçli bir karar — ölçek ve amaç uyuşmazlığı,
  bizim "basitlik" ilkemize aykırı şişme riski. `DECISIONS.md`'ye ayrıca yazılmadı çünkü
  bu yeni bir mimari karar değil, mevcut kararların (vektör hafıza yok, multi-agent yok)
  yeniden teyidi.

## Sonraki Oturuma Not

Sıradaki iş net: **M7 — gerçek proje testi**, adımları `PLANS.md`'de yazılı. Özet:
`C:\Users\batuh\proje-a` oluştur, `git init`, hub'dan `.\install.ps1 -Target ..\proje-a`,
`<!-- DOLDUR: -->` alanlarını doldur, projede gerçek küçük bir şey geliştir, sonra
handoff/resume döngüsünü orada dene.

Açık kalan bir soru var: **proje-a'da ne geliştirileceği belirlenmedi.** Bomboş bir
projede handoff/resume testi yapay kalır (anlatacak bir "durum" olmaz) — küçük ama
gerçek bir şey (basit bir script, todo uygulaması vb.) olmalı. Kullanıcıya soruldu,
cevap alınmadan oturum kapandı.

Beklenen bulgu: `doctor` ve `.githooks/pre-commit` install ile hedefe kopyalanmıyor —
gerçek kullanımda ilk fark edilecek eksik bu olacak.

## İlgili Dosyalar

`skills/handoff/SKILL.md` (iki yeni kural), `docs/DECISIONS.md` (son kayıt),
`PLANS.md` § M7 (sıradaki işin tam adımları).
