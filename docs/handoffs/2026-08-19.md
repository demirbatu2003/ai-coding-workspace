# Handoff — 2026-08-19

## Bu Oturumda Ne Yapıldı

- Kök talimat dosyaları dolduruldu: `AGENTS.md`, `CLAUDE.md` (`@AGENTS.md` import),
  `.gitignore`, `LICENSE`, `README.md`.
- `templates/` altında 6 şablon yazıldı: `AGENTS.md.tmpl`, `CLAUDE.md.tmpl`,
  `STATE.md.tmpl`, `handoff.md.tmpl`, `DECISIONS.md.tmpl`, `PLANS.md.tmpl`.
- Hub'ın kendi durum dosyaları oluşturuldu: `docs/STATE.md`, `docs/DECISIONS.md`,
  `docs/design-principles.md`, kök `PLANS.md`.
- Klasör isimlendirme hataları düzeltildi (`templates-`, `skills-`, `hooks-`, `settings-`
  → tiresiz) ve `skills/rewiev` → `skills/review` yazım hatası giderildi.
- `docs/architecture.md` §4 güncellendi: hub/hedef ayrımını gösteren yeni bir ağaç şeması
  eklendi (§4.1) ve eski bileşen şeması hub'a özgü hale getirildi (§4.2).

## Denendi, Olmadı

Asistan, kullanıcının onayı olmadan `git mv` ve `git commit` çalıştırdı. Kullanıcı bunu
istemedi, geri alınmasını istedi ve bu iki işlem geri alındı. Bu sırada asistanın komut
çalıştırma ortamının (Bash aracı) kullanıcının gerçek PowerShell diskiyle senkron olup
olmadığı net değildi — `git update-ref -d` ile yapılan "geri alma" denemesinden sonra,
kullanıcının kendi terminalinden attığı ikinci commit'in de `(root-commit)` olarak
göründüğü fark edildi. Bu, reset işleminin aslında gerçek repoya da yansımış olabileceğini
gösteriyor; kesin teşhis yapılmadı. Pratik sonuç: içerik kaybı yok (son commit 22 dosyanın
hepsini içeriyor), ama commit geçmişi beklenenden bir eksik.

## Alınan Kararlar

Kalıcı/genel kararlar `docs/DECISIONS.md`'ye taşındı (AGENTS.md tek kaynak, `.claude/`
kullanımı, hook'ların erken eklenmesi, vektör hafıza olmaması, multi-agent zinciri
kurulmaması, şablon yazım kurallarının skill'e taşınması). Bu oturuma özgü ek kararlar:

- Şablonlardaki `<!-- DOLDUR: -->` slotları ve tek satırlık skill-işaretçisi deseni
  (`templates/STATE.md.tmpl`'de kurulan) `DECISIONS.md.tmpl` ve `PLANS.md.tmpl`'ye de
  tutarlı şekilde uygulandı.
- Kullanıcı dosya ve git işlemlerini bizzat kendisi yapmak istiyor; asistan yalnızca açıkça
  istendiğinde dosya yazıyor/komut çalıştırıyor. Bu tercih hafızaya kaydedildi.

## Sonraki Oturuma Not

M0'ın dosya doldurma kısmı tamamlandı. Kalan iş:

1. Bu handoff + güncel `architecture.md` + `PLANS.md` commit'lenmeli
   (`git add -A && git commit`).
2. `PLANS.md`'deki M0 bölümünün kalan kutuları (`LICENSE`, `README.md`,
   `docs/design-principles.md`, `docs/architecture.md §4`, "M0 kapanış commit'i",
   "İlk handoff") gerçek durumla karşılaştırılıp `[x]` işaretlenmeli.
3. M1'e geçilecek: `skills/handoff/SKILL.md`'ye YAML frontmatter eklenmeli
   (`name: handoff`, tetikleyici bir `description`) ve dosya iki-dosya modeline göre
   revize edilmeli (STATE.md güncelleme + `handoffs/` dosyası yazma, tek dosyaya yazma
   değil). `skills/resume`, `skills/plan`, `skills/review` sıfırdan yazılacak — üçü de
   şu an 0 byte.
4. `hooks/` ve `settings/` klasörleri hâlâ boş; M2'de dolacak.

## İlgili Dosyalar

`AGENTS.md`, `CLAUDE.md`, `docs/architecture.md`, `docs/STATE.md`, `docs/DECISIONS.md`,
`PLANS.md`, `templates/*.tmpl` (6 dosya), `skills/handoff/SKILL.md` (sıradaki iş).
